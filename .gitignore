*.iml
.gradle/
.kotlin/
/local.properties
/.idea/
.DS_Store
/build
*/build
**/build/
/captures
.externalNativeBuild
.cxx
local.properties

# Release signing material must never be committed. Losing the key means never
# being able to update the app again, so back it up somewhere outside this
# folder - but never into git.
*.jks
*.keystore
/keystore/
keystore.properties

# Intermediate audio from the ringtone generator; only .ogg is committed.
tools/ringtone-gen/**/*.wav
tools/ringtone-gen/preview/

# Working notes and local tooling. Kept out of the public repository.
/research/
/.claude/
