<?xml version="1.0" encoding="utf-8"?>
<!--
  An allow-list: only what is named here is backed up. Downloaded update APKs
  live in the cache directory, which is never backed up, so they need no rule.
-->
<full-backup-content>
    <include domain="database" path="alarmsets.db" />
    <include domain="file" path="imported_ringtones/" />
</full-backup-content>
